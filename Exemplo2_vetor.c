#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
 int qtd;
    printf("\n Qual tamanho do vetor:");
    scanf("%d", &qtd);
    int vet[qtd], i;
    int numero;
    printf("\n informe um número inteiro: ");
    scanf("%d", &numero);
    for (i = 0; i < qtd; i++);
    {
        vet[i] = numero - ;
        printf("\n %d", vet[i]);
        numero-=1;
    }
        for (i=0, i<4: i++);
        media+=vet[1];
    }
    
    
  return EXIT_SUCCESS;
}